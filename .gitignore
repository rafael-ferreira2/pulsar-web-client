node_modules/
tokens.sh

# Raw screen recording — too large for normal git pushes (GitHub rejects
# anything > 100 MB without LFS). The compressed walkthrough that the README
# embeds lives at media/clips/00-full-demo.mp4 and IS committed.
# Re-generate via `./media/make-clips.sh` whenever the source changes.
media/ScreenRecording.mov

# Smoke-test scratch output (regenerated by `node test/export-smoke.js --dump`).
test/sample-output/
